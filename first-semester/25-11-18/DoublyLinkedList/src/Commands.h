#pragma once

enum Command {
    PUSH_BACK = 0,
    PUSH_FRONT = 1,
    PRINT = 2,
    CLEAR = 3,
    EMPTY = 4,
    INSERT_AFTER = 5,
    INSERT_BEFORE = 6,
    FRONT = 7,
    BACK = 8,
    GET = 9,
    SET = 10,
    EXIT = 11
};
